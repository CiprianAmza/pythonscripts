function uniDecoder(...arr) {
  
  let result = "";

  for (let i = 0; i < arr.length; i++) {
    if (typeof arr[i] === "number") {
      result += String.fromCharCode(arr[i]);
    }
  }

  if (result === "") {
    return "not a valid character code";
  }

  return result;
}
