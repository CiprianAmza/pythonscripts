const crossover = (a, b, j)=> {

  let c = "", d = "";
  
  for (let i = 0; i < a.length; i++) {
     
    if (i < j) {
    
     c += a[i];
     d += b[i];
    } else {
     c += b[i];
     d += a[i];
    }
  }
  
  return [c, d];
};
