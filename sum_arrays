def sum_arrays(a, s):
    
    ans = []
    q = 0
    for arr in a:
        for i in range(len(arr)):
            if len(ans) <= (q+i):
                ans.append(arr[i])
            else:
                ans[q+i] += arr[i]
        q += s
    return ans
