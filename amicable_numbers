def get_divs(n):
    
    divs = set()
    for i in range(1, int(n**.5)+1):
        if n % i == 0:
            divs.add(i)
            divs.add(n//i)
    
    return sum(divs)

def amicable_numbers(n1,n2):

    return get_divs(n1) == get_divs(n2)
