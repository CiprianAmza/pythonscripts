function areEqual(s1, s2){
  
  for (let el of s1)
    if (!s2.has(el)) return false;
  for (let el of s2)
    if (!s1.has(el)) return false;
  
  return true;
}

function notEqual(s1, s2){
  
  return !areEqual(s1, s2);
}
