function zipObject(keys, values = []) { 
  
  let obj = {};

  if (!(keys[0] instanceof Array)) {
    for (let i = 0; i < keys.length; i++) {
      obj[keys[i]] = values[i];
    }
  } else {
    for (let key of keys) {
      obj[key[0]] = key[1];
    }
  }
  
  return obj;
}
