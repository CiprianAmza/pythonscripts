def transform(x):
    return max(min(x, 2.375), 0)

def passer_rating(att, yds, comp, td, ints):
    
    a = ((comp / att) - .3) * 5
    b = ((yds / att) - 3) * .25
    c = (td / att) * 20
    d = 2.375 - ((ints / att) * 25)
    
    return round(sum(map(transform, [a,b,c,d])) / 6 * 100, 1)
